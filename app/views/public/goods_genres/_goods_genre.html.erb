 <div class="card">
  <div class= "card-header">
   <h5 class= "card-title">ジャンル検索</h5>
 </div>
 <div class="card-body">
         <div class="goods_genres">
        <% goods_genres.each do |goods_genre| %>
          <p>
       <%= link_to goods_genre.genre_name,items_path(goods_genre_id: goods_genre.id), class: "card-link text-secondary"%> 
          </p>
      <% end %>
   </div>
  </div>
 </div>